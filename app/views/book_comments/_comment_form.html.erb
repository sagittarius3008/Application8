<!--from books/show.html book:@book-->

      
      <!--コメントの投稿-->
      <div class="comments">
        <table>
          <% book_comments.each do |book_comment| %>
          <tr>
            <!--image_tagは書評の投稿者(book)ではなく、コメントの投稿者にする。引数はbook_comment.user-->
            <td>
              <%= attachment_image_tag(book_comment.user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
            </td>
            <td><%= book_comment.user.name %></td>
            <td><%= book_comment.created_at.strftime('%Y/%m/%d') %><%= book_comment.comment %></td>
              <% if book_comment.user == current_user %>
                <td class="comment-delete">
                  <%= link_to "削除", book_book_comment_path(book_comment.book, book_comment), method: :delete %>
                </td>
              <% end %>
            <% end %>
          </tr>
        </table>
      </div>
      
      <!--コメントフォーム-->
      
    </div>
  </div>
</div><!--<form action="/books/1/book_comments/1",method="post"><input name="utf8" type="hidden" value="&#x2713;" />-->




<!--<%#= form_with(model:[book, @book_comment], remote: true) do |f| %>-->
<!--  <%#= f.text_area :comment, rows:'6',placeholder: "コメントをここに" %>-->
<!--  <%#= f.submit "送信する" %>-->
<!--<%# end %>-->